//==============================================================================
// SparkSL texture input
//==============================================================================
vec4 main(in function<vec4(vec2)> noise) {
  vec2 uv = std::getVertexTexCoord();
  return noise(uv);
}
